<Window x:Class="AMDownloader.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
        xmlns:local="clr-namespace:AMDownloader"
        xmlns:properties="clr-namespace:AMDownloader.Properties"
        mc:Ignorable="d"
        Title="Options" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" ShowInTaskbar="False" SizeToContent="WidthAndHeight" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Label Content="Max _parallel downloads*:" TabIndex="0"/>
            <TextBox VerticalAlignment="Center" HorizontalContentAlignment="Right" Text="{Binding Source={x:Static properties:Settings.Default}, Path=MaxParallelDownloads}" Width="50" TabIndex="1"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0 10 0 0" >
            <Label Content="*Requires application restart" />
        </StackPanel>
        <StackPanel Margin="0 10 0 0"  HorizontalAlignment="Right" Orientation="Horizontal" Height="25" Grid.Row="2">
            <Button x:Name="btnOK" Content="_OK" Width="80" Margin="0 0 5 0" IsDefault="True" Click="btnOK_Click" Command="{Binding SaveSettingsCommand}" TabIndex="2"/>
            <Button Content="_Cancel" Width="80" IsCancel="True" TabIndex="3"/>
        </StackPanel>
    </Grid>
</Window>
